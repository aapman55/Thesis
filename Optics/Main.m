% Magnification and image location
close all; clear all; clc;

stdColors = lines;

[~,~,~, AX, H1, H2] = calculateDIM(0.1,2);


calculateDIM(0.2,1);

[~,~,~, AX, H1, H2] = calculateDIM(0.1,2);

calculateDIM(0.2,2);